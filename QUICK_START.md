# 🚀 Sirbit Website - Quick Start Guide

## تشغيل المشروع في دقائق

### 1️⃣ تشغيل السيرفر

```bash
npm run dev
```

افتح المتصفح على: http://localhost:3000

> سيتم توجيهك تلقائيًا إلى `/ar` (العربية)

---

## 🔄 تبديل اللغة

**من خلال الواجهة:**
- اضغط على زر "EN" في الناف بار للتبديل للإنجليزية
- اضغط على زر "AR" للعودة للعربية

**مباشرة من الرابط:**
- العربية: `http://localhost:3000/ar`
- الإنجليزية: `http://localhost:3000/en`

---

## 🌓 تبديل الثيم

اضغط على أيقونة الشمس/القمر في الناف بار

- 🌙 القمر = الوضع الليلي
- ☀️ الشمس = الوضع النهاري

---

## 📄 الصفحات المتاحة

### الرئيسية
- `/ar` أو `/en`
- تحتوي: Hero, من نحن, الخدمات, لماذا Sirbit, الأعمال, التقنيات, CTA

### الخدمات
- `/ar/services` أو `/en/services`
- تحتوي: تفاصيل كل خدمة (Web, Mobile, WordPress)

### الأعمال
- `/ar/portfolio` أو `/en/portfolio`
- تحتوي: فلاتر (الكل, ويب, موبايل, WordPress) + مشاريع

### تفاصيل مشروع
- `/ar/portfolio/project-1` أو `/en/portfolio/project-1`
- تحتوي: تفاصيل المشروع الكاملة

### من نحن
- `/ar/about` أو `/en/about`
- تحتوي: الرؤية والرسالة، طريقة العمل

### تواصل
- `/ar/contact` أو `/en/contact`
- تحتوي: نموذج تواصل + معلومات التواصل

---

## ✏️ تعديل المحتوى

### النصوص

كل النصوص موجودة في:
- `locales/ar.json` (العربي)
- `locales/en.json` (الإنجليزي)

**مثال:**
```json
// locales/ar.json
{
  "hero": {
    "title": "نحو تجارب رقمية أسرع، أذكى، وأبسط."
  }
}
```

### الألوان

في `app/globals.css`:
```css
--primary-blue: #00AEEF;
--primary-purple: #7B61FF;
--accent-turquoise: #29C6B7;
```

### الصور

ضع الصور في: `public/images/`

استخدمها في الكود:
```tsx
<Image src="/images/your-image.jpg" alt="..." />
```

---

## 🎨 المكونات الجاهزة

### Button
```tsx
import Button from '@/components/ui/Button';

<Button href="/contact" variant="primary">
  اتصل بنا
</Button>
```

### SectionHeader
```tsx
import SectionHeader from '@/components/ui/SectionHeader';

<SectionHeader 
  title="العنوان" 
  subtitle="الوصف" 
/>
```

### IconCard
```tsx
import IconCard from '@/components/ui/IconCard';

<IconCard 
  icon={<Icon />}
  title="العنوان"
  description="الوصف"
/>
```

---

## 🏗️ إضافة صفحة جديدة

1. أنشئ ملف في: `app/[locale]/your-page/page.tsx`
2. أضف الترجمات في: `locales/ar.json` و `locales/en.json`
3. أضف رابط في الناف بار: `components/layout/Navbar.tsx`

**مثال:**
```tsx
// app/[locale]/blog/page.tsx
export default function BlogPage() {
  return (
    <Container>
      <h1>المدونة</h1>
    </Container>
  );
}
```

---

## 🛠️ أوامر مهمة

```bash
# تشغيل السيرفر
npm run dev

# بناء المشروع
npm run build

# تشغيل النسخة النهائية
npm start

# فحص الأخطاء
npm run lint
```

---

## 🔧 تخصيصات سريعة

### تغيير اللغة الافتراضية

في `middleware.ts`:
```ts
defaultLocale: 'en' // بدل من 'ar'
```

### تغيير الثيم الافتراضي

في `app/[locale]/layout.tsx`:
```tsx
<ThemeProvider defaultTheme="dark"> {/* بدل من "light" */}
```

### إضافة لغة جديدة

1. أضف ملف: `locales/fr.json`
2. في `middleware.ts`: `locales: ['ar', 'en', 'fr']`
3. في `i18n/request.ts`: `const locales = ['ar', 'en', 'fr']`

---

## 📱 تجربة الموبايل

في المتصفح:
1. اضغط F12
2. اختر أيقونة الموبايل (Device Toolbar)
3. جرب على مقاسات مختلفة

---

## 🚀 النشر

### على Vercel (موصى به)

```bash
npm install -g vercel
vercel deploy
```

### على Netlify

```bash
npm run build
# ارفع مجلد .next
```

---

## ❓ مشاكل شائعة

### المشروع لا يعمل؟
```bash
rm -rf .next node_modules
npm install
npm run dev
```

### الترجمة لا تظهر؟
تأكد أن المفتاح موجود في `locales/ar.json` و `locales/en.json`

### الصور لا تظهر؟
تأكد أن الصورة موجودة في `public/images/` واستخدم المسار الصحيح

---

## 📞 الدعم

أي استفسار، راجع:
- `README.md` - التوثيق الكامل
- `PROJECT_SUMMARY.md` - ملخص المشروع
- مجلد `plan/` - المواصفات الأصلية

---

**صنع بحب من فريق Sirbit** ❤️

